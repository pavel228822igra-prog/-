# Инструкция по загрузке проекта на GitHub

## Репозиторий уже инициализирован и готов!

Проект подготовлен для GitHub. Теперь нужно создать репозиторий на GitHub и отправить код.

## Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите кнопку **"New"** (или перейдите на https://github.com/new)
3. Заполните:
   - **Repository name:** `health-tracker` (или любое другое имя)
   - **Description:** "Мобильное приложение для учета здоровья с симуляцией данных"
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
4. Нажмите **"Create repository"**

## Шаг 2: Отправьте код на GitHub

После создания репозитория GitHub покажет инструкции. Выполните в терминале:

```bash
cd "/Users/pskripilev/Downloads/Мои файлы/healt"

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin git@github.com:YOUR_USERNAME/health-tracker.git

# Или если используете HTTPS:
# git remote add origin https://github.com/YOUR_USERNAME/health-tracker.git

# Отправьте код
git branch -M main
git push -u origin main
```

## SSH или HTTPS?

### Если используете SSH (рекомендуется):

SSH ссылка будет выглядеть так:
```
git@github.com:YOUR_USERNAME/health-tracker.git
```

**Проверьте, есть ли у вас SSH ключ:**
```bash
ls -la ~/.ssh/id_rsa.pub
```

**Если нет SSH ключа, создайте его:**
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
# Нажмите Enter для всех вопросов

# Скопируйте публичный ключ
cat ~/.ssh/id_rsa.pub
```

**Добавьте SSH ключ на GitHub:**
1. Скопируйте вывод команды выше
2. Зайдите на https://github.com/settings/keys
3. Нажмите **"New SSH key"**
4. Вставьте ключ и сохраните

### Если используете HTTPS:

HTTPS ссылка будет выглядеть так:
```
https://github.com/YOUR_USERNAME/health-tracker.git
```

При первом push может потребоваться авторизация (личный токен доступа).

## Готовые команды (замените YOUR_USERNAME):

```bash
cd "/Users/pskripilev/Downloads/Мои файлы/healt"

# Для SSH:
git remote add origin git@github.com:YOUR_USERNAME/health-tracker.git
git branch -M main
git push -u origin main

# Или для HTTPS:
git remote add origin https://github.com/YOUR_USERNAME/health-tracker.git
git branch -M main
git push -u origin main
```

## Проверка статуса

После отправки проверьте:
```bash
git remote -v
git status
```

## Что уже в репозитории:

✅ Все исходные файлы  
✅ Конфигурация проекта  
✅ Документация (opisanie_po.md, README.md и др.)  
✅ .gitignore настроен правильно  
✅ Первый коммит создан  

## Важно:

В `.gitignore` уже исключены:
- `node_modules/` - зависимости
- `.expo/` - временные файлы Expo
- `.DS_Store` - системные файлы macOS

Эти файлы **не** будут загружены на GitHub (что правильно).

---

**После выполнения команд ваш проект будет на GitHub!** 🚀


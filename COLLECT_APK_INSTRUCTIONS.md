# Инструкция для сборки APK

К сожалению, я не могу выполнить сборку APK автоматически, так как для этого требуется:

1. **Авторизация в Expo** - нужен интерактивный вход с логином и паролем
2. **Глобальная установка EAS CLI** - требует прав администратора
3. **Сборка происходит на серверах Expo** - занимает 10-15 минут

## Но вы можете собрать APK самостоятельно за 3 шага:

### Шаг 1: Установите EAS CLI

В вашем терминале выполните:

```bash
npm install -g eas-cli
```

Если возникнут проблемы с правами, используйте:

```bash
sudo npm install -g eas-cli
```

### Шаг 2: Войдите в Expo

```bash
eas login
```

Введите email и пароль (или создайте новый аккаунт - это бесплатно).

### Шаг 3: Соберите APK

```bash
cd "/Users/pskripilev/Downloads/Мои файлы/healt"
eas build --platform android --profile preview
```

Сборка займет 10-15 минут. После завершения вы получите ссылку для скачивания APK файла.

---

## Альтернативный вариант: Использовать npx (без глобальной установки)

```bash
cd "/Users/pskripilev/Downloads/Мои файлы/healt"

# Сначала войдите
npx eas-cli@latest login

# Затем соберите
npx eas-cli@latest build --platform android --profile preview
```

---

## Что уже готово для сборки:

✅ Конфигурация `eas.json` создана  
✅ `app.json` настроен  
✅ Все зависимости установлены  
✅ Проект готов к сборке  

Осталось только выполнить команды выше! 🚀

